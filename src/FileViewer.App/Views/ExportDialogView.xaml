<Window x:Class="FileViewer.App.Views.ExportDialogView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Export" Height="420" Width="520" WindowStartupLocation="CenterOwner">
    <DockPanel Margin="10">
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Format:" VerticalAlignment="Center" Margin="0,0,8,0" />
            <RadioButton Content="DIF" GroupName="Format" IsChecked="{Binding IsDifSelected}" Margin="0,0,8,0" />
            <RadioButton Content="CSV" GroupName="Format" IsChecked="{Binding IsCsvSelected}" Margin="0,0,8,0" />
            <RadioButton Content="TSV" GroupName="Format" IsChecked="{Binding IsTsvSelected}" Margin="0,0,8,0" />
            <RadioButton Content="JSON" GroupName="Format" IsChecked="{Binding IsJsonSelected}" />
        </StackPanel>
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Export..." Width="90" Click="OnExportClick" Margin="0,0,8,0" />
            <Button Content="Cancel" Width="90" Click="OnCancelClick" />
        </StackPanel>
        <TextBlock DockPanel.Dock="Top" Text="Preview (first 2 rows):" Margin="0,0,0,4" />
        <TextBox Text="{Binding PreviewText, Mode=OneWay}" IsReadOnly="True" FontFamily="Consolas"
                 TextWrapping="NoWrap" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" />
    </DockPanel>
</Window>
